{/*
================================================================================
TEMPLATE: Category / Subcategory Overview Page
================================================================================

Purpose
-------
Each category and component subcategory has an overview.md that:
1. Introduces what kinds of nodes live here.
2. Documents patterns shared by ALL nodes in this group (saves repetition
   on every per-node page).
3. Lists every node in the group as a table linking to its page.

Output location
---------------
- docs/Flow/Nodes/<Category>/overview.md         (e.g. Events/overview.md)
- docs/Flow/Nodes/Components/<Component>/overview.md (e.g. Rigidbody/overview.md)
- docs/Flow/Nodes/Math/<Type>/overview.md         (e.g. Vector3/overview.md)

When to skip
------------
For component subfolders with only 2–3 simple nodes (potentially VideoPlayer,
Light), an overview.md may be overkill — the autogenerated category index
will list the pages without prose. Decide per case.

How to use this file
--------------------
1. Copy to the target overview.md path.
2. Strip ALL `{/* ... */}` block comments.
3. Fill in: intro paragraphs, optional shared-pattern note, the node table.
4. Set `sidebar_position: 0` so the overview appears first in the sidebar
   above the individual node pages.
*/}

---
sidebar_position: 0
{/* Optional: sidebar_label: "Overview"  — keeps the sidebar entry short */}
---

# {/* Category or Component Name — e.g. "Event Nodes" or "Rigidbody Nodes" */}

{/* For Unity-component overviews (Animator, Rigidbody, AudioSource, etc.),
    add a one-line link to the Unity component class page directly under the
    H1, before the intro paragraphs. Skip for non-component overviews
    (Events, Logic, Variables, AI, Voice, Interactables, etc.).

**Unity reference:** [↗](https://docs.unity3d.com/ScriptReference/Rigidbody.html)
*/}

{/* 1–3 short paragraphs:
    - What kinds of nodes live in this category/subcategory.
    - Any patterns that apply to ALL nodes in the group. Document them here
      once instead of repeating on every per-node page. Examples:
        * "All Action nodes have a Flow input on top-left and a Flow output
           on top-right."
        * "All Rigidbody nodes require a Rigidbody component on the target
           GameObject — the node will no-op if missing."
        * "All Net variants of these nodes fire on every player in the
           session — see Networking in Flow."
    - Cross-link relevant concepts: [Networking in Flow](/docs/Flow/flow-networking),
      [Variables](/docs/Flow/flow-variables), [Logic nodes](/docs/Flow/Nodes/Logic/overview).
*/}

{/* Optional: a single category-wide screenshot showing the shared shape
    of nodes in this group. Use sparingly — only when it's genuinely
    illustrative.
![{/* alt */}](/img/flow/{/* existing-image */}.png)
*/}

{/* Optional admonition for a category-wide tip or gotcha
:::tip
Short tip that applies to every node in this category.
:::
*/}

## Nodes

{/* Table listing every node in the group. Use the "Net" column ONLY in
    categories where Net variants exist (Events, some Actions). Skip it
    for Math, Logic, etc. */}

| Node | Description | Net |
|---|---|---|
| [{/* Node Name */}](./{/* node-slug */}) | {/* one-line description */} | [Net version](./net-{/* node-slug */}) |
| [{/* Node Name */}](./{/* node-slug */}) | {/* one-line description */} | — |

{/* For node families with many near-identical variants, group them into a
    sub-table or a compact pattern instead of one row per variant.
    See Appendix A of flow-node-template.md. Example for codegen wrappers
    (Vector3, Mathf, etc.) — note the `Unity ref` column linking to the
    ScriptReference page for each wrapped method:

    ## Vector3 operations

    All Vector3 math nodes take Vector3 inputs and return the operation's
    natural output type. Behavior matches `UnityEngine.Vector3` exactly.

    | Node | Inputs | Output | Unity ref |
    |---|---|---|---|
    | Vector3 Angle           | a, b               | Float   | [↗](https://docs.unity3d.com/ScriptReference/Vector3.Angle.html) |
    | Vector3 ClampMagnitude  | vector, maxLength  | Vector3 | [↗](https://docs.unity3d.com/ScriptReference/Vector3.ClampMagnitude.html) |
    | Vector3 Cross           | a, b               | Vector3 | [↗](https://docs.unity3d.com/ScriptReference/Vector3.Cross.html) |
    | Vector3 Distance        | a, b               | Float   | [↗](https://docs.unity3d.com/ScriptReference/Vector3.Distance.html) |
    | Vector3 Lerp            | a, b, t            | Vector3 | [↗](https://docs.unity3d.com/ScriptReference/Vector3.Lerp.html) |

    Drop the `Unity ref` column entirely if any row in the table lacks a
    Unity equivalent — don't leave em-dashes. For hand-coded node families
    (e.g. Logic, Events), use the simpler form without the column:

    | Node | Operand type |
    |---|---|
    | Add Int     | Int     |
    | Add Float   | Float   |
    | Add Vector3 | Vector3 |
*/}

{/*
================================================================================
APPENDIX — Voice & style cheatsheet (same as per-node template)
================================================================================

- Match existing Flow docs voice: conversational second person, short
  paragraphs.
- Bold key terms on first mention.
- Internal links absolute: /docs/Flow/Nodes/...
*/}
